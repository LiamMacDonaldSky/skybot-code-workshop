/**
 * Challenge 2: Array Intersection
 *
 * Write a function that takes two arrays and returns a new array containing
 * only the elements that appear in both arrays (the intersection).
 *
 * Each element in the result should appear only once, even if it appears
 * multiple times in the input arrays.
 *
 * Examples:
 * Input: [1, 2, 3, 4] and [3, 4, 5, 6]
 * Output: [3, 4]
 *
 * Input: ["a", "b", "c"] and ["b", "c", "d"]
 * Output: ["b", "c"]
 */

export function arrayIntersection<T>(arr1: T[], arr2: T[]): T[] {
  // TODO: Implement your solution here
  throw new Error("Not implemented");
}
